dependsOn(':model')

configure(jar.osgi) {
  instruction 'Import-Package', '*'
  instruction 'Export-Package', 'de.guderlei.pubsub.hub.*'
  instruction 'Bundle-Activator', 'de.guderlei.pubsub.hub.activator.Activator'
}

dependencies {
	compile project(':model')
}
