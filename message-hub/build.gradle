dependsOn(':model')

configure(jar.osgi) {
  instruction 'Import-Package', '*'
  instruction 'Bundle-Module', 'de.guderlei.pubsub.hub.activator.HubModule'
}

dependencies {
	compile project(':model')
}
