simple and small OSGi demo
- some bundles
- declarative services
- guice peaberry
- integration tests (pax exam)
- Configuration of declarative services with ConfigAdmin/MetaType

Maven 3 is required to build the demo.

There is also a Pax Runner config:
pax-run --args=file:etc/runner.properties
